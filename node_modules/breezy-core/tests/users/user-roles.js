'use strict'

const breezyConfig = require('../../data/config')
require('../../src/util/breezy-config').initConfig(breezyConfig)
const mongo = require('../../src/db/mongoClient')()
const helpers = require('../../src/util/helpers')
const memberClient = require('../../src/lib/company/core-member')


const roles = ['manager', 'position_member']

async function main() {

  await mongo

  //await memberClient.addCompanyRoleToUser('e16af8b6eb68', 'a123456', 'admin')
  await memberClient.removeCompanyRoleFromUser('e16af8b6eb68', 'a123456', 'admin')


}

function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}


setTimeout(main, 1500);
