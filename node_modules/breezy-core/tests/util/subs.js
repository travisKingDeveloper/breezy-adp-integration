'use strict'



let simple = 'hey dog, I see what is going down. this is a bit longer to test some of the stuff that could happen with a bigger message and stuff... and things and thigns with the stuff that dfgdfgdfghrghwrtlkgntgobit wergwotrgt'

let fs = require('fs');
let data

try {
  data = fs.readFileSync('testfile.txt', 'utf8');
  console.log(data.length);
} catch(e) {
  console.log('Error:', e.stack);
}

let time = new Date()

let message = ''
let result

for (let i = 0; i < 100; i++) {
  for (let x = 0; x < 10000; x++) {
    //message = message + simple
    if (data.length > 500)
      result = data.slice(0, 500)
    //result = message.substring(0, 1024)
    //result = message.substr(0, 1024)
    //console.log(message.length)
  }
  //console.log('pass', new Date() - time, 'ms')
}

let sliceTime = new Date() - time
console.log('slice Final', sliceTime, 'ms')
console.log(result)

time = new Date()
message = ''

for (let i = 0; i < 100; i++) {
  for (let x = 0; x < 10000; x++) {
    //message = message + simple
    //result = message.slice(0, 1024)
    if (data.length > 500)
      result = data.substring(0, 500)
    //result = message.substr(0, 1024)
    //console.log(message.length)
  }
  //console.log('pass', new Date() - time, 'ms')
}

let substringTime = new Date() - time
console.log('substring Final', new Date() - time, 'ms')
console.log(result)

time = new Date()
message = ''

for (let i = 0; i < 100; i++) {
  for (let x = 0; x < 10000; x++) {
    //message = message + simple
    //result = message.slice(0, 1024)
    //result = message.substring(0, 1024)
    if (data.length > 500)
      result = data.substr(0, 500)
    //console.log(message.length)
  }
  //console.log('pass', new Date() - time, 'ms')
}

let substrTime = new Date() - time
console.log('substr Final', substrTime, 'ms')

console.log(result)


console.log('**********')
console.log('*', 'sliceTime', sliceTime)
console.log('*', 'substringTime', substringTime)
console.log('*', 'substrTime', substrTime)
console.log('**********')