'use strict'

const breezy_config = require('../../data/breezy-config')
let _config

module.exports = {
  getConfig: getConfig,
  initConfig: init
}

function init(config) {

  if (config) {
    _config = breezy_config.mergeConfigs(config)
  }
}

function getConfig() {
  return _config
}